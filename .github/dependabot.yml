version: 2
updates:
  # The crate itself has no dependencies; these are the workflow actions.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: "ci"
